CONFIG_ZMK_POINTING=y

# The pinnacle driver also requires this to include sleep code
CONFIG_ZMK_MOUSE=y

CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# Deep sleep disabled for now — was causing intermittent host BLE drops /
# reconnects on ZMK main (sys_poweroff tears down the radio). Idle-only
# power savings remain via CONFIG_ZMK_IDLE_TIMEOUT below.
# CONFIG_ZMK_SLEEP=y
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Idle after 30s (trackpad / radio power). Unchanged from Toucan defaults.
CONFIG_ZMK_IDLE_TIMEOUT=30000

# Soft-off implies PM device suspend/resume (needed for Cirque + display).
CONFIG_ZMK_PM_SOFT_OFF=y
